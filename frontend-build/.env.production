# Production build config — baked into the Netlify build by Vite.
# The Clerk publishable key is public by design (it ships in the browser
# bundle); the secret key lives only in Render env vars.
# Netlify dashboard env vars, if set, override these values.
VITE_API_URL=https://roof-mri-backend.onrender.com
VITE_CLERK_PUBLISHABLE_KEY=pk_live_Y2xlcmsucHJvcG9zYWxzLnJvb2YtbXJpLmNvbSQ
