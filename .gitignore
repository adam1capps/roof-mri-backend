node_modules/
.env
.env.local
.env.production
*.log
.DS_Store

# Frontend production build config contains only PUBLIC values
# (publishable key + API URL) and must be committed for Netlify builds
!frontend-build/.env.production
